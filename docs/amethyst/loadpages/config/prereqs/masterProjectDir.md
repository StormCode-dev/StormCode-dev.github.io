# MasterProjectDir

```
#best used if isMaster is false. auto-points to your intended root directory. IGNORED if isMaster is true
masterProjectDir = C:\path\torepo
```

Used if isMaster is false. Gives a point of resolution for Amethyst to check to.