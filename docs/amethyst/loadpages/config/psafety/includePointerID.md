# includePointerID

```
#pointers look like '--MOVEDTO-->' normally. if this option is true, it might look like '--MOVEDTO[ecj8lm]-->'
#valid choices: never, always
includePointerID=always
```

Specifies if the pointer ID should be included. Recommended for use, as it will deliminate and fix issues that you could run into.