# HashAlgorithm

```
#if blake3 isnt available, or unreasonable, you can choose a different hash algorithm to be used
#valid choices: blake3, blake2b, sha256, sha1
hashAlgorithm=blake3
```

Blake3 is the standard, and is very fast. You can however switch to another algorithm if this is not desirable.